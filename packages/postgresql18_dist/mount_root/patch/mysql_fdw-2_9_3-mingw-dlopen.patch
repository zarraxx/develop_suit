diff --git a/Makefile b/Makefile
--- a/Makefile
+++ b/Makefile
@@ -31,7 +31,8 @@ else
 DLSUFFIX = .so
 endif
 
-PG_CPPFLAGS += -D _MYSQL_LIBNAME=\"lib$(MYSQL_LIB)$(DLSUFFIX)\"
+MYSQL_LIBNAME ?= lib$(MYSQL_LIB)$(DLSUFFIX)
+PG_CPPFLAGS += -D _MYSQL_LIBNAME=\"$(MYSQL_LIBNAME)\"
 
 ifdef USE_PGXS
 PG_CONFIG = pg_config
diff --git a/mysql_fdw.c b/mysql_fdw.c
--- a/mysql_fdw.c
+++ b/mysql_fdw.c
@@ -22,6 +22,13 @@
 #include <sys/stat.h>
 #include <unistd.h>
 
+#ifndef RTLD_LAZY
+#define RTLD_LAZY 0
+#endif
+#ifndef RTLD_DEEPBIND
+#define RTLD_DEEPBIND 0
+#endif
+
 #include "access/htup_details.h"
 #include "access/sysattr.h"
 #include "access/reloptions.h"
