#!/usr/bin/env bash
set -euo pipefail

filtered_args=()
for arg in "$@"; do
  case "$arg" in
    -p|--preserve-dates)
      ;;
    *)
      filtered_args+=("$arg")
      ;;
  esac
done

exec "@STRIP@" "${filtered_args[@]}"
