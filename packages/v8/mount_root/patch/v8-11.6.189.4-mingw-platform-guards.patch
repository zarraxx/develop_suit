diff --git a/src/base/bits.h b/src/base/bits.h
index 2d61878..0fd0dad 100644
--- a/src/base/bits.h
+++ b/src/base/bits.h
@@ -17,6 +17,15 @@
 #include "src/base/win32-headers.h"
 #endif
 
+#if defined(__MINGW32__)
+#ifdef RotateRight32
+#undef RotateRight32
+#endif
+#ifdef RotateLeft32
+#undef RotateLeft32
+#endif
+#endif
+
 namespace v8 {
 namespace base {
 namespace bits {
diff --git a/src/base/platform/platform-win32.cc b/src/base/platform/platform-win32.cc
index 152fbbf..e313145 100644
--- a/src/base/platform/platform-win32.cc
+++ b/src/base/platform/platform-win32.cc
@@ -18,7 +18,11 @@
 #include <windows.h>
 
 // This has to come after windows.h.
+#if defined(__MINGW32__)
+#include <versionhelpers.h>
+#else
 #include <VersionHelpers.h>
+#endif
 #include <dbghelp.h>  // For SymLoadModule64 and al.
 #include <malloc.h>   // For _msize()
 #include <mmsystem.h>  // For timeGetTime().
@@ -64,7 +68,7 @@ static_assert(offsetof(V8_CRITICAL_SECTION, SpinCount) ==
 
 // Extra functions for MinGW. Most of these are the _s functions which are in
 // the Microsoft Visual Studio C++ CRT.
-#ifdef __MINGW32__
+#if defined(__MINGW32__) && !defined(__MINGW64_VERSION_MAJOR)
 
 
 #ifndef __MINGW64_VERSION_MAJOR
@@ -141,6 +145,19 @@ bool g_hard_abort = false;
 
 }  // namespace
 
+#if defined(__MINGW32__)
+bool IsWindows10OrGreater() {
+  OSVERSIONINFOEXW osvi = {};
+  osvi.dwOSVersionInfoSize = sizeof(osvi);
+  osvi.dwMajorVersion = 10;
+
+  DWORDLONG condition_mask = 0;
+  VER_SET_CONDITION(condition_mask, VER_MAJORVERSION, VER_GREATER_EQUAL);
+
+  return VerifyVersionInfoW(&osvi, VER_MAJORVERSION, condition_mask) != FALSE;
+}
+#endif
+
 class WindowsTimezoneCache : public TimezoneCache {
  public:
   WindowsTimezoneCache() : initialized_(false) {}
diff --git a/src/base/platform/time.cc b/src/base/platform/time.cc
index b6da0a6..fd7a59d 100644
--- a/src/base/platform/time.cc
+++ b/src/base/platform/time.cc
@@ -790,8 +790,9 @@ bool ThreadTicks::IsSupported() {
 #elif defined(__PASE__)
   // Thread CPU time accounting is unavailable in PASE
   return false;
-#elif(defined(_POSIX_THREAD_CPUTIME) && (_POSIX_THREAD_CPUTIME >= 0)) || \
-    defined(V8_OS_DARWIN) || defined(V8_OS_ANDROID) || defined(V8_OS_SOLARIS)
+#elif ((defined(_POSIX_THREAD_CPUTIME) && (_POSIX_THREAD_CPUTIME >= 0)) || \
+       defined(V8_OS_DARWIN) || defined(V8_OS_ANDROID) || \
+       defined(V8_OS_SOLARIS)) && !defined(V8_OS_WIN)
   return true;
 #elif defined(V8_OS_WIN)
   return IsSupportedWin();
@@ -816,8 +817,8 @@ ThreadTicks ThreadTicks::Now() {
   return ThreadTicks(ComputeThreadTicks());
 #elif V8_OS_FUCHSIA
   return ThreadTicks(GetFuchsiaThreadTicks());
-#elif(defined(_POSIX_THREAD_CPUTIME) && (_POSIX_THREAD_CPUTIME >= 0)) || \
-  defined(V8_OS_ANDROID)
+#elif ((defined(_POSIX_THREAD_CPUTIME) && (_POSIX_THREAD_CPUTIME >= 0)) || \
+       defined(V8_OS_ANDROID)) && !defined(V8_OS_WIN)
   return ThreadTicks(ClockNow(CLOCK_THREAD_CPUTIME_ID));
 #elif V8_OS_SOLARIS
   return ThreadTicks(gethrvtime() / Time::kNanosecondsPerMicrosecond);
diff --git a/src/profiler/heap-snapshot-generator.cc b/src/profiler/heap-snapshot-generator.cc
index 04f22b3..ead0337 100644
--- a/src/profiler/heap-snapshot-generator.cc
+++ b/src/profiler/heap-snapshot-generator.cc
@@ -412,7 +412,7 @@ HeapSnapshot::HeapSnapshot(HeapProfiler* profiler,
   static_assert(kSystemPointerSize != 4 || sizeof(HeapGraphEdge) == 12);
   static_assert(kSystemPointerSize != 8 || sizeof(HeapGraphEdge) == 24);
   static_assert(kSystemPointerSize != 4 || sizeof(HeapEntry) == 32);
-#if V8_CC_MSVC
+#if V8_CC_MSVC || defined(__MINGW32__)
   static_assert(kSystemPointerSize != 8 || sizeof(HeapEntry) == 48);
 #else   // !V8_CC_MSVC
   static_assert(kSystemPointerSize != 8 || sizeof(HeapEntry) == 40);
